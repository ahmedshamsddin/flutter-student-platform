## To run backend:
`npm install`
`npm run dev`

## Also use .env.example to set .env variables:
Windows:
`copy .env.example .env`
Linux:
`cp .env.example .env`

## To run frontend:
`flutter pub get`
`flutter run -v`
